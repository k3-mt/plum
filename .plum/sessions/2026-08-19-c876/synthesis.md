# Seams — session 2026-08-19-c876

_Composed mechanically from bundle.json with no model in the loop. Every
line below is derived from an AST fact; nothing here is inferred intent._

## What changed, in one paragraph

497 symbols across 51 files: 497 added, 0 modified, 0 deleted. 224 new items entered the public surface.

## Assumptions this code makes

- `internal/cli/pipeline.go::cmdTrace` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/cli/pipeline.go::cmdTrace` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/cli/pipeline.go::cmdQuiz` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/cli/pipeline.go::cmdClaims` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/cli/root.go::Version` assumes no two callers (or two tests) mutate this package-level state at once
    - first thing that breaks: a parallel test run, which is where this surfaces first
- `internal/server/server.go::Server.Serve` assumes the goroutine finishes before anything observes its effect
    - first thing that breaks: a flaky test, then a race in production
- `internal/server/server.go::Server.Serve` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/server/server.go::Server.handleAsk` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/server/server.go::Server.handleAsk` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/server/server.go::Server.handleDone` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/server/server.go::writeJSON` assumes callers pass the right dynamic type, because the compiler no longer checks
    - first thing that breaks: unclear from the AST alone — this is where a trace would answer it
- `internal/server/server.go::writeJSON` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/server/server.go::openBrowser` assumes the remote side always answers, and answers quickly
    - first thing that breaks: a hung request holding its caller open indefinitely
- `internal/server/server.go::openBrowser` assumes the remote side always answers, and answers quickly
    - first thing that breaks: a hung request holding its caller open indefinitely
- `internal/server/server.go::openBrowser` assumes the remote side always answers, and answers quickly
    - first thing that breaks: a hung request holding its caller open indefinitely
- `internal/server/server.go::openBrowser` assumes the discarded error can never be the one that mattered
    - first thing that breaks: a silent wrong answer instead of a loud failure
- `internal/synth/anthropic.go::Anthropic.Complete` assumes the input is small enough to hold in memory
    - first thing that breaks: memory growth proportional to whatever the other side sends
- `cmd/plum/main.go::main` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/bundle/bundle.go::Write` has 6 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/bundle/bundle.go::Read` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/capture/capture.go::MarkStart` has 9 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/claims/claims.go::Render` has 14 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/claims/verify.go::packageName` has 10 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/claims/verify.go::tail` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/m0.go::cmdInit` has 20 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/m0.go::cmdRun` has 19 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/m0.go::cmdMark` has 20 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/m0.go::cmdNote` has 14 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/m0.go::cmdReport` has 9 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/m0.go::cmdLs` has 15 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/m0.go::cmdShow` has 9 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/pipeline.go::cmdStale` has 10 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/pipeline.go::cmdLandscape` has 18 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/pipeline.go::cmdClaims` has 21 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/root.go::Main` has 9 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/root.go::usage` has 16 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/cli/root.go::newEnv` has 10 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/config/config.go::Config.apply` has 19 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/config/toml.go::ParseTOML` has 19 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/config/toml.go::parseValue` has 13 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/config/toml.go::unquote` has 6 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/explore/telemetry.go::Store.Append` has 11 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/explore/telemetry.go::Store.Load` has 8 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/explore/telemetry.go::Store.LoadMisses` has 8 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/explore/telemetry.go::topN` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/extract/deps.go::Extractor.deps` has 9 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/extract/deps.go::parseGoMod` has 12 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/extract/deps.go::parseRequirements` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/extract/divergence.go::Extractor.divergence` has 26 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/extract/divergence.go::accumulate` has 7 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/extract/extract.go::Extractor.risks` has 7 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/extract/extract.go::Extractor.coverage` has 14 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/extract/extract.go::isTestFile` has 8 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/generic/adapter.go::Adapter.declarations` has 8 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/generic/adapter.go::Adapter.ParseSymbols` has 19 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/generic/adapter.go::Adapter.commentsIn` has 6 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/generic/adapter.go::Adapter.callSitesIn` has 10 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/generic/adapter.go::Adapter.PublicSurface` has 9 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/generic/adapter.go::Adapter.RiskMarkers` has 6 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/generic/adapter.go::Adapter.CallEdges` has 8 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/gopkg/adapter.go::Adapter.funcSymbol` has 17 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/gopkg/adapter.go::Adapter.genSymbols` has 28 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/gopkg/adapter.go::receiverName` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/gopkg/adapter.go::signature` has 11 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/lang/gopkg/adapter.go::bodyComments` has 9 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/quiz/quiz.go::normalise` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/quiz/quiz.go::argString` has 7 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/report/report.go::Render` has 116 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/report/report.go::isTest` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/server/server.go::New` has 11 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/server/server.go::Server.buildContext` has 8 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/server/server.go::Server.handleSymbol` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/server/server.go::Server.handleAsk` has 14 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/server/server.go::renderContext` has 46 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/server/server.go::Server.handleTelemetry` has 8 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/server/server.go::Server.handleDone` has 6 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/server/server.go::writeJSON` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/store/store.go::Store.WriteFile` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/synth/anthropic.go::Anthropic.Complete` has 14 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/synth/offline.go::Offline.Complete` has 58 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/synth/offline.go::surfaceSentence` has 6 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/synth/offline.go::failureModes` has 15 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/synth/offline.go::lookFirst` has 5 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/synth/offline.go::offlineClaims` has 12 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/synth/synth.go::extractClaims` has 15 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/trace/collect.go::modulePath` has 10 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/trace/landscape.go::formatArgs` has 6 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/trace/schema.go::WriteFile` has 6 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong
- `internal/vcs/git.go::Repo.git` has 8 outbound calls and no declaration doc — every assumption it makes is unstated
    - first thing that breaks: the next reader reconstructs them from the body, and gets one wrong

## Invariants

- `cmd/plum/main.go::main` now depends on `internal/cli/root.go::Main` across a module boundary — that dependency direction must stay acyclic
- `internal/bundle/bundle.go::Read` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture.go::Close` now depends on `internal/vcs/git.go::Repo.RevParse` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture.go::Close` now depends on `internal/vcs/git.go::Repo.Snapshot` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture.go::LoadMark` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture.go::MarkStart` now depends on `internal/vcs/git.go::Repo.RevParse` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture.go::Run` now depends on `internal/journal/journal.go::Harvest` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture.go::Run` now depends on `internal/vcs/git.go::Repo.RevParse` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture_test.go::TestUncommittedWorkIsCapturedAndTheWorkingTreeIsUntouched` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture_test.go::TestUncommittedWorkIsCapturedAndTheWorkingTreeIsUntouched` now depends on `internal/vcs/git.go::Repo.Show` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture_test.go::TestUncommittedWorkIsCapturedAndTheWorkingTreeIsUntouched` now depends on `internal/vcs/git.go::Repo.WorkingTreeDirty` across a module boundary — that dependency direction must stay acyclic
- `internal/capture/capture_test.go::gitRepo` now depends on `internal/config/config.go::Default` across a module boundary — that dependency direction must stay acyclic
- `internal/claims/claims.go::Load` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/claims/claims.go::Parse` now depends on `internal/bundle/types.go::SymbolID` across a module boundary — that dependency direction must stay acyclic
- `internal/claims/claims.go::Parse` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/claims/claims.go::Render` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/claims/verify.go::Verify` now depends on `internal/bundle/bundle.go::SymbolID.File` across a module boundary — that dependency direction must stay acyclic
- `internal/claims/verify.go::Verify` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/claims/verify.go::Verify` now depends on `internal/trace/collect.go::copyTree` across a module boundary — that dependency direction must stay acyclic
- `internal/claims/verify.go::safe` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/bundle/bundle.go::Bundle.Has` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/claims/verify.go::Verify` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/config/config.go::Default` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/explore/telemetry.go::NewStore` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/explore/telemetry.go::TargetSymbols` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/extract/extract.go::Extractor.Extract` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/lang/adapter.go::NewRegistry` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/quiz/quiz.go::Generate` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/quiz/quiz.go::Grade` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/store/store.go::Store.ClaimsPath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/trace/landscape.go::Derive` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/trace/landscape.go::DeriveChain` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/vcs/git.go::Repo.RevParse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/e2e_test.go::TestPipelineEndToEnd` now depends on `internal/vcs/git.go::Repo.git` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdGate` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdLs` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdLs` now depends on `internal/store/store.go::Store.List` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdMark` now depends on `internal/capture/capture.go::ClearMark` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdMark` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdMark` now depends on `internal/capture/capture.go::LoadMark` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdMark` now depends on `internal/capture/capture.go::MarkStart` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdMark` now depends on `internal/extract/extract.go::Extractor.Extract` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdMark` now depends on `internal/journal/journal.go::Harvest` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdMark` now depends on `internal/store/store.go::Store.BundlePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdNote` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRange` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRange` now depends on `internal/capture/capture.go::SessionID` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRange` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRange` now depends on `internal/extract/extract.go::Extractor.Extract` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRange` now depends on `internal/journal/journal.go::Harvest` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRange` now depends on `internal/store/store.go::Store.BundlePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRange` now depends on `internal/vcs/git.go::Repo.RevParse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdReport` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdReport` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRun` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRun` now depends on `internal/extract/extract.go::Extractor.Extract` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdRun` now depends on `internal/store/store.go::Store.BundlePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdShow` now depends on `internal/bundle/bundle.go::Write` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdShow` now depends on `internal/store/store.go::Store.BundlePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdShow` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::cmdShow` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::renderReport` now depends on `internal/stale/stale.go::Check` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::renderReport` now depends on `internal/store/store.go::Store.ClaimsPath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::renderReport` now depends on `internal/trace/landscape.go::Landscape.Notes` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/m0.go::renderReport` now depends on `internal/trace/landscape.go::Landscape.UnannotatedExpensive` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdClaims` now depends on `internal/claims/verify.go::Verify` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdClaims` now depends on `internal/store/store.go::StateDir` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdClaims` now depends on `internal/store/store.go::Store.ClaimsPath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdClaims` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/explore/telemetry.go::NewStore` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/server/server.go::Server.Serve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/store/store.go::StateDir` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/store/store.go::Store.ClaimsPath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/store/store.go::Store.SynthesisPath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/store/store.go::Store.TracePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/synth/anthropic.go::NewAnthropic` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdExplore` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdLandscape` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdLandscape` now depends on `internal/store/store.go::Store.LandscapePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdLandscape` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdLandscape` now depends on `internal/store/store.go::Store.TracePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdLandscape` now depends on `internal/trace/landscape.go::Chain` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdLandscape` now depends on `internal/trace/landscape.go::DeriveChain` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdLandscape` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/explore/telemetry.go::NewStore` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/explore/telemetry.go::Store.AppendMiss` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/explore/telemetry.go::Store.IsDone` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/explore/telemetry.go::Store.LoadMisses` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/explore/telemetry.go::TargetSymbols` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/quiz/quiz.go::Generate` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/quiz/quiz.go::Grade` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/quiz/quiz.go::MissFor` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/quiz/quiz.go::Summarise` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/store/store.go::StateDir` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/store/store.go::Store.TracePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdQuiz` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdStale` now depends on `internal/stale/stale.go::Check` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdStale` now depends on `internal/store/store.go::Store.ClaimsPath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdStale` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdSynth` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdSynth` now depends on `internal/store/store.go::Store.ClaimsPath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdSynth` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdSynth` now depends on `internal/store/store.go::Store.SynthesisPath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdSynth` now depends on `internal/vcs/git.go::Repo.Diff` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdTrace` now depends on `internal/claims/claims.go::Parse` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdTrace` now depends on `internal/store/store.go::StateDir` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdTrace` now depends on `internal/store/store.go::Store.LandscapePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdTrace` now depends on `internal/store/store.go::Store.Resolve` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdTrace` now depends on `internal/store/store.go::Store.TracePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdTrace` now depends on `internal/store/store.go::Store.TracesDir` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdTrace` now depends on `internal/trace/landscape.go::Chain` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::cmdTrace` now depends on `internal/trace/landscape.go::DeriveChain` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::loadLandscape` now depends on `internal/store/store.go::Store.LandscapePath` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::loadLandscape` now depends on `internal/trace/landscape.go::LoadLandscape` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::printLandscape` now depends on `internal/trace/landscape.go::Landscape.Notes` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::printLandscape` now depends on `internal/trace/landscape.go::Landscape.UnannotatedExpensive` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/pipeline.go::providerFor` now depends on `internal/synth/anthropic.go::NewAnthropic` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/root.go::newEnv` now depends on `internal/vcs/git.go::Repo.Root` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/root.go::registry` now depends on `internal/lang/adapter.go::NewRegistry` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/root.go::registry` now depends on `internal/lang/generic/adapter.go::Python` across a module boundary — that dependency direction must stay acyclic
- `internal/cli/root.go::registry` now depends on `internal/lang/generic/adapter.go::TypeScript` across a module boundary — that dependency direction must stay acyclic
- `internal/config/config.go::Load` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/explore/telemetry.go::Store.Append` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/explore/telemetry.go::Store.AppendMiss` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/explore/telemetry.go::Store.Load` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/explore/telemetry.go::Store.LoadMisses` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/deps.go::Extractor.deps` now depends on `internal/lang/gopkg/adapter.go::parse` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/deps.go::Extractor.deps` now depends on `internal/vcs/git.go::Repo.Show` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/divergence.go::Extractor.divergence` now depends on `internal/config/config.go::Config.Forbids` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/divergence.go::Extractor.sampleBaseline` now depends on `internal/vcs/git.go::Repo.ListFiles` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/divergence.go::Extractor.sampleBaseline` now depends on `internal/vcs/git.go::Repo.Show` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/extract.go::Extractor.Extract` now depends on `internal/bundle/bundle.go::Bundle.Sort` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/extract.go::Extractor.Extract` now depends on `internal/lang/adapter.go::Registry.Language` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/extract.go::Extractor.Extract` now depends on `internal/vcs/git.go::Repo.Diff` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/extract.go::Extractor.Extract` now depends on `internal/vcs/git.go::Repo.NameStatus` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/extract.go::Extractor.Extract` now depends on `internal/vcs/git.go::Repo.Show` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/extract.go::Extractor.Extract` now depends on `internal/vcs/git.go::Repo.Status` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/extract.go::Extractor.coverage` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/extract.go::Extractor.edges` now depends on `internal/bundle/bundle.go::SymbolID.File` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/golden_test.go::TestGoldenBundles` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/golden_test.go::copyDir` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/golden_test.go::extractFixture` now depends on `internal/config/config.go::Default` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/golden_test.go::extractFixture` now depends on `internal/lang/adapter.go::NewRegistry` across a module boundary — that dependency direction must stay acyclic
- `internal/extract/golden_test.go::extractFixture` now depends on `internal/vcs/git.go::Repo.RevParse` across a module boundary — that dependency direction must stay acyclic
- `internal/journal/journal.go::Append` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/journal/journal.go::Harvest` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/generic/adapter.go::Adapter.CallEdges` now depends on `internal/bundle/types.go::SymbolID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/generic/adapter.go::Adapter.ParseSymbols` now depends on `internal/bundle/bundle.go::MakeID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/generic/adapter.go::Adapter.callSitesIn` now depends on `internal/bundle/bundle.go::MakeID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::Adapter.Comments` now depends on `internal/extract/diff.go::Hunk.End` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::Adapter.Normalise` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::Adapter.funcSymbol` now depends on `internal/bundle/bundle.go::MakeID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::Adapter.funcSymbol` now depends on `internal/extract/diff.go::Hunk.End` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::Adapter.genSymbols` now depends on `internal/bundle/bundle.go::MakeID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::Adapter.genSymbols` now depends on `internal/extract/diff.go::Hunk.End` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::bodyComments` now depends on `internal/extract/diff.go::Hunk.End` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::callSites` now depends on `internal/bundle/bundle.go::MakeID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::commentIndex` now depends on `internal/extract/diff.go::Hunk.End` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::parsed.text` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/adapter.go::signature` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/risk.go::Adapter.CallEdges` now depends on `internal/bundle/bundle.go::MakeID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/risk.go::Adapter.CallEdges` now depends on `internal/bundle/types.go::SymbolID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/risk.go::Adapter.RiskMarkers` now depends on `internal/bundle/bundle.go::MakeID` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/risk.go::exprString` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/lang/gopkg/surface.go::Adapter.PublicSurface` now depends on `internal/bundle/bundle.go::MakeID` across a module boundary — that dependency direction must stay acyclic
- `internal/quiz/quiz.go::Generate` now depends on `internal/bundle/bundle.go::SymbolID.Qualified` across a module boundary — that dependency direction must stay acyclic
- `internal/quiz/quiz.go::labels` now depends on `internal/bundle/bundle.go::SymbolID.Qualified` across a module boundary — that dependency direction must stay acyclic
- `internal/report/report.go::Render` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.Serve` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.buildContext` now depends on `internal/bundle/bundle.go::Bundle.EdgesFrom` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.buildContext` now depends on `internal/bundle/bundle.go::Bundle.EdgesTo` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.buildContext` now depends on `internal/bundle/bundle.go::Bundle.JournalFor` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.buildContext` now depends on `internal/bundle/bundle.go::Bundle.Lookup` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.buildContext` now depends on `internal/bundle/bundle.go::Bundle.RisksFor` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.handleDone` now depends on `internal/explore/telemetry.go::Store.MarkDone` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.handleLandscape` now depends on `internal/trace/landscape.go::Landscape.Notes` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.handleLandscape` now depends on `internal/trace/landscape.go::Landscape.UnannotatedExpensive` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.handleSymbol` now depends on `internal/bundle/types.go::SymbolID` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::Server.source` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::renderContext` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server.go::writeJSON` now depends on `internal/cli/m0.go::multiFlag.Set` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server_test.go::TestDoneUnlocksInterrogation` now depends on `internal/explore/telemetry.go::Store.IsDone` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server_test.go::testServer` now depends on `internal/config/config.go::Default` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server_test.go::testServer` now depends on `internal/explore/telemetry.go::NewStore` across a module boundary — that dependency direction must stay acyclic
- `internal/server/server_test.go::testServer` now depends on `internal/trace/landscape.go::Derive` across a module boundary — that dependency direction must stay acyclic
- `internal/stale/stale.go::Check` now depends on `internal/bundle/bundle.go::SymbolID.File` across a module boundary — that dependency direction must stay acyclic
- `internal/stale/stale.go::Check` now depends on `internal/trace/schema.go::ReadFile` across a module boundary — that dependency direction must stay acyclic
- `internal/stale/stale_test.go::setup` now depends on `internal/config/config.go::Default` across a module boundary — that dependency direction must stay acyclic
- `internal/stale/stale_test.go::setup` now depends on `internal/lang/adapter.go::NewRegistry` across a module boundary — that dependency direction must stay acyclic
- `internal/store/store.go::Store.Dir` now depends on `internal/config/config.go::Config.SessionDir` across a module boundary — that dependency direction must stay acyclic
- `internal/store/store.go::Store.List` now depends on `internal/config/config.go::Config.SessionsDir` across a module boundary — that dependency direction must stay acyclic
- `internal/store/store.go::Store.Load` now depends on `internal/bundle/bundle.go::Read` across a module boundary — that dependency direction must stay acyclic
- `internal/store/store.go::Store.Save` now depends on `internal/bundle/bundle.go::Write` across a module boundary — that dependency direction must stay acyclic
- `internal/store/store_test.go::seed` now depends on `internal/config/config.go::Default` across a module boundary — that dependency direction must stay acyclic
- `internal/synth/anthropic.go::Anthropic.Complete` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/synth/anthropic.go::Anthropic.Complete` now depends on `internal/cli/m0.go::multiFlag.Set` across a module boundary — that dependency direction must stay acyclic
- `internal/synth/anthropic.go::Anthropic.Complete` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/synth/offline.go::Offline.Complete` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/synth/synth.go::Brief` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/synth/synth.go::extractClaims` now depends on `internal/bundle/bundle.go::Bundle.Fingerprints` across a module boundary — that dependency direction must stay acyclic
- `internal/synth/synth.go::extractClaims` now depends on `internal/bundle/types.go::SymbolID` across a module boundary — that dependency direction must stay acyclic
- `internal/synth/synth_test.go::TestOfflineSynthesisIsGroundedInTheBundle` now depends on `internal/config/config.go::Default` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/collect.go::Collector.Run` now depends on `internal/bundle/bundle.go::SymbolID.File` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/collect.go::Collector.Run` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/collect.go::copyTree` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/collect.go::inject` now depends on `internal/bundle/bundle.go::SymbolID.Qualified` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/landscape.go::DeriveChain` now depends on `internal/bundle/bundle.go::Bundle.CallSiteComment` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/landscape.go::DeriveChain` now depends on `internal/bundle/bundle.go::Bundle.HasRisk` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/landscape.go::DeriveChain` now depends on `internal/bundle/bundle.go::Bundle.Lookup` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/schema.go::ReadFile` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/trace/schema.go::WriteFile` now depends on `internal/capture/capture.go::Close` across a module boundary — that dependency direction must stay acyclic
- `internal/vcs/git.go::Repo.Snapshot` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic
- `internal/vcs/git.go::Repo.git` now depends on `internal/cli/m0.go::multiFlag.String` across a module boundary — that dependency direction must stay acyclic

## Failure modes

- **Concurrent caller**: `internal/cli/root.go::Version` is package-level state shared by every caller in the process.
- **Concurrent caller**: `internal/server/server.go::Server.Serve` starts a goroutine nothing waits on — ordering is unobservable.
- **Dependency returns an error**: `internal/cli/pipeline.go::cmdClaims` discards it at internal/cli/pipeline.go:438, so the caller sees success.
- **Dependency returns an error**: `internal/cli/pipeline.go::cmdQuiz` discards it at internal/cli/pipeline.go:375, so the caller sees success.
- **Dependency returns an error**: `internal/cli/pipeline.go::cmdTrace` discards it at internal/cli/pipeline.go:151, so the caller sees success.
- **Dependency returns an error**: `internal/cli/pipeline.go::cmdTrace` discards it at internal/cli/pipeline.go:167, so the caller sees success.
- **Dependency returns an error**: `internal/server/server.go::Server.Serve` discards it at internal/server/server.go:73, so the caller sees success.
- **Dependency returns an error**: `internal/server/server.go::Server.handleAsk` discards it at internal/server/server.go:200, so the caller sees success.
- **Dependency returns an error**: `internal/server/server.go::Server.handleAsk` discards it at internal/server/server.go:220, so the caller sees success.
- **Dependency returns an error**: `internal/server/server.go::Server.handleDone` discards it at internal/server/server.go:355, so the caller sees success.
- **Dependency returns an error**: `internal/server/server.go::openBrowser` discards it at internal/server/server.go:381, so the caller sees success.
- **Dependency returns an error**: `internal/server/server.go::writeJSON` discards it at internal/server/server.go:368, so the caller sees success.
- **Malformed or oversized input**: `internal/synth/anthropic.go::Anthropic.Complete` reads without a bound.
- **Slow or unavailable network**: `internal/server/server.go::openBrowser` has no deadline at internal/server/server.go:375.
- **Slow or unavailable network**: `internal/server/server.go::openBrowser` has no deadline at internal/server/server.go:377.
- **Slow or unavailable network**: `internal/server/server.go::openBrowser` has no deadline at internal/server/server.go:379.

## Where to look first if this breaks

1. `internal/server/server.go::openBrowser` — subprocess_without_context — exec.Command has no context — the child outlives a cancelled caller
2. `internal/cli/pipeline.go::cmdTrace` — swallowed_error — result assigned to _ — a returned error is dropped here
3. `internal/server/server.go::Server.handleAsk` — swallowed_error — result assigned to _ — a returned error is dropped here

## Rationale

- `internal/vcs/git.go`: session end snapshots build a commit against a throwaway GIT_INDEX_FILE: stash create refuses to run once intent-to-add entries exist, and cannot see untracked files at all
    - rejected: git stash create, as the spec's pseudocode suggested
    - rejected: shelling out to git stash push/pop, which disturbs the working tree
- `internal/lang/generic/adapter.go`: Go ships native go/ast, so M0 needs no grammar at all; the line-based fallback covers Python and TS honestly until the wazero spike is worth its day
    - rejected: tree-sitter via cgo bindings
    - rejected: tree-sitter grammars as WASM under wazero

```plum-claims
assertion :: internal/cli/pipeline.go::cmdTrace :: the error discarded at line 151 can never be actionable
assertion :: internal/cli/pipeline.go::cmdTrace :: the error discarded at line 167 can never be actionable
assertion :: internal/cli/pipeline.go::cmdQuiz :: the error discarded at line 375 can never be actionable
assertion :: internal/cli/pipeline.go::cmdClaims :: the error discarded at line 438 can never be actionable
executable :: internal/cli/root.go::Version :: calling this twice in one process gives the same result as calling it once
assertion :: internal/server/server.go::Server.Serve :: the goroutine's effect is observed before anything depends on it
assertion :: internal/server/server.go::Server.Serve :: the error discarded at line 73 can never be actionable
assertion :: internal/server/server.go::Server.handleAsk :: the error discarded at line 200 can never be actionable
assertion :: internal/server/server.go::Server.handleAsk :: the error discarded at line 220 can never be actionable
assertion :: internal/server/server.go::Server.handleDone :: the error discarded at line 355 can never be actionable
assertion :: internal/server/server.go::writeJSON :: the error discarded at line 368 can never be actionable
assertion :: internal/server/server.go::openBrowser :: the error discarded at line 381 can never be actionable
```
